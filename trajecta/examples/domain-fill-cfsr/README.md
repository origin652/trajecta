# Domain-fill CFSR example

This is the executable project used by the fifteen-minute quickstart and the
domain-fill moisture tutorial. Copy the four CFSR demonstration files into
`data/`, then run `project finalize` explicitly.
