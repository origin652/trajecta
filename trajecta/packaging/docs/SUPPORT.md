# Supported release environments

| Platform | Architecture | Product status |
|---|---|---|
| Windows | x86_64 | Supported release package |
| Ubuntu 24.04 | x86_64 | Supported release package |

The Rust reader is the default. The packaged native reader is available on the
two listed platforms. Other systems may be suitable for contributor builds but
are not release-supported during the alpha series.
