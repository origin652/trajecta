schema_version: trajecta.project-index/v1
name: minimal-cfsr
cases:
  demo: cases/demo.yaml
profiles:
  local:
    path: profiles/local.yaml
    dataset_profiles:
      cfsr: cfsr-pgbl-pressure-v0
default_profile: local
